# The built app contains the question bank — it has to be able to grade offline.
# That is not a cryptographic secret and does not pretend to be: anyone who opens
# DevTools sees it. But the route by which someone who is NOT looking for it would
# find it is a Google search, and that is what gets closed here. No indexing, no
# snippets in results, no cache.
#
# This applies to the public deployment of the app (Cloudflare Pages). The code
# repository does not contain the bank and does not need this restriction.
User-agent: *
Disallow: /

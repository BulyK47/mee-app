<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Same value as `background_color` in the web manifest, so the launch screen, the strips
         behind the system bars on an older WebView, and the app itself are one colour. -->
    <color name="splashBackground">#0A0E12</color>
</resources>
